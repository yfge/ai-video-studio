你正在修复已完整生成的结构化章节大纲，只允许返回一次最小伏笔补丁。
必须按模板顺序让每个点名 thread_id 恰好出现一次；不得改名、新增、遗漏，未点名的有效调度由系统原样保留。
每章最多回收 {{ max_payoffs_per_chapter }} 条。系统已经确定性分配且预留唯一 assigned_payoff_position；必须逐字复制模板中的位置，禁止沿用旧位置或改章。
evidence_key_event 的完整允许值已经写在模板中：必须逐字复制，不得补充任何新人物、地点、时刻、物件、动作或因果。系统会把它追加为目标章 key_event。
只返回严格 JSON，不得重输 chapters 或解释：{{ repair_template_json }}
repair_context：{{ repair_context_json }}
确定性诊断：{{ validation_error }}
