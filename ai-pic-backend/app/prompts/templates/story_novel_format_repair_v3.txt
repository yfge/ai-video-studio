上一响应没有通过确定性验证。必须修复下面的具体错误，不得原样返回失败响应。

验证错误（最高优先级）：
{{ error }}

修复规则：
1. 只使用原任务给出的当前章合同、状态和允许实体，不得新增未来事件、状态变化或实体。
2. 若错误指出字段缺失或“不能为空”，必须补出符合原任务的非空值；这属于完成原合同，
   不是改写合同语义。不得保留空字符串、null、空数组占位。
3. 若错误指出 hash、ID、顺序或合同绑定不匹配，必须逐字复制原任务要求的值。
4. 保留原响应中已经满足合同的内容，只修改导致验证失败的字段和必要的 JSON 结构。
5. 返回前逐项核对验证错误已经消失；不要解释、道歉或输出 Markdown。

原任务：
{{ original_prompt }}

失败响应：
{{ raw_response }}

再次核对并修复：{{ error }}
只返回原任务要求的严格 JSON。
