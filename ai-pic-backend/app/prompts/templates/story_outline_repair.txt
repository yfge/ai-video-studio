你是一个严格的故事大纲审校器。请将输出修复为符合 StoryOutlineModel Schema 的纯 JSON。

- Schema: {{ schema }}
- 缺失/异常字段: {{ missing_fields | default([]) | join(', ') }}
- production_mode: {{ production_mode | default(false) }}
- 质量门禁问题ID: {{ quality_gate_issues | default([]) | join(', ') }}
- 质量门禁问题详情:
{% for item in quality_gate_issue_details | default([]) %}
  - {{ item }}
{% endfor %}
- 故事质量警告: {{ story_quality_warnings | default([]) | join('；') }}
- 角色一致性警告: {{ character_warnings | default([]) | join('；') }}
- 原始提示词:
{{ original_prompt or '' }}
- 原始输出:
{{ original_output }}

要求：
1) 只返回 JSON，不要代码块或额外文字。
{% if production_mode %}
2) 必须只返回完整的 story_seed_v1 envelope。
{% else %}
2) 必须包含 premise、synopsis、main_conflict、resolution、character_relationships、main_characters 等兼容字段，缺失时用合理占位（中文）补齐。
{% endif %}
3) 尽量保留原始信息，确保字段类型与 Schema 一致。
{% if production_mode %}
4) 只补齐 `story_seed`（schema=`story_seed_v1`）：title、premise、outline、protagonists、world_constraints、central_conflict、ending_direction、target_audience、content_constraints。
5) protagonists 只能引用原始提示词中给定的 virtual_ip_business_id。
6) 不要补写 structured_story_contract、前三集结构、阶段高潮、投流钩子或拍摄计划。
{% endif %}
