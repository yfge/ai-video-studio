description: Compile the frozen schema and initial fact graph for a long V5 story
category: story
version: "1.3"
variables:
  contract_json: { required: true, type: string }
  output_contract_json: { required: true, type: string }
