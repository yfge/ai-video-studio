你是一个严格的剧集规划审校器。请将输出修复为符合 EpisodePlanModel Schema 的纯 JSON。

- Schema: {{ schema }}
- 缺失/异常字段: {{ missing_fields | default([]) | join(', ') }}
- 原始提示词:
{{ original_prompt or '' }}
- 原始输出:
{{ original_output }}

要求：
1) 只返回 JSON，不要代码块或额外文字。
2) episodes 必须是数组；每个元素包含 episode_number、title、summary、plot_points（数组，含 description/timing 可选）、character_arcs（对象）、conflicts（数组，含 type/description/intensity 可选）、scene_count（整数，可推断）。
3) 尽量保留原始信息；缺失字段合理补齐占位（中文）。
