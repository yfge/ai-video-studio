修复超长 Story 的一致性基础编译结果。

冻结输入：{{ contract_json }}
上次输出：{{ previous_json }}
确定性诊断：{{ diagnostics_json }}
输出 JSON Schema：{{ output_contract_json }}

只修复诊断指出的结构、引用、类型、谓词持久级别、条件或效果问题。不得改写 StorySeed，不得加入逐章 causal_event_graph，不得输出解释、hash、source_manifest 或诊断。

规则对象必须使用扁平结构：op、predicate_id、subject、value、scope、perspective_id 直接位于 condition/effect 对象顶层；op 是每条规则必填字段，禁止嵌套 fact/condition/effect。事件角色只能写成 {"role":"已声明角色名"}。例如：{"op":"exists","predicate_id":"p","subject":{"role":"actor"}}；{"op":"assert","predicate_id":"p","subject":{"role":"actor"},"value":true,"scope":"objective"}。all/any 使用 args，not 使用 arg，event_before 使用 event_id。

必须逐项满足上述 JSON Schema 的 required、enum 与 type；不得自行发明同义字段或旧字段。只输出 JSON：
{"consistency_schema":{"schema":"story_novel_consistency_schema.v1","version":1,"entity_types":[],"predicates":[],"event_types":[],"constraints":[],"perspectives":[],"source_manifest":{}},"initial_fact_graph":{"schema":"story_novel_fact_graph.v1","entities":[],"facts":[],"evidence":[],"occurred_event_ids":[]}}
