---
id: 2025-12-08T16-30-46Z-lint-selenium
date: 2025-12-08T16:30:46Z
participants: [human, codex]
models: [gpt-4o]
tags: [frontend, backend, testing]
related_paths:
  - ai-pic-frontend/src/app/environments/page.tsx
  - ai-pic-frontend/src/app/episodes/[id]/storyboard/page.tsx
  - ai-pic-backend/requirements-test.txt
  - ai-pic-backend/tests/unit/test_openai_unit.py
summary: "Resolve ESLint <img> warnings with next/image, add selenium test dependency, fix OpenAI unit test SyntaxError."
---

## User Prompt
处理残留 ESLint <img> 警告，安装 selenium 并修复导致 pytest 报错的 SyntaxError，确保测试可运行。

## Goals
- 替换前端分镜与环境列表中的 `<img>` 为 `next/image` 以通过 lint。
- 为后端测试添加 selenium 依赖，解决缺失导致的 ImportError。
- 修复 `tests/unit/test_openai_unit.py` 的 f-string 转义 SyntaxError。

## Changes
- 前端：`environments/page.tsx` 和 `storyboard/page.tsx` 统一使用 `next/image`（fill + unoptimized）展示参考图，去除 lint 警告。
- 后端：在 `requirements-test.txt` 新增 `selenium==4.26.1` 并安装；修正 OpenAI 单测中 PNG 头校验的 f-string，避免语法错误。

## Validation
- `docker exec ai-video-frontend npm run lint` ✅（仅剩锁文件提示，无 lint 警告）。
- `docker exec ai-video-backend pytest tests/api/test_role_management.py -q --maxfail=1` ✅（通过，仍有 pytest ReturnNotNone 警告）。
- 未跑全量 pytest（涉及外部依赖/浏览器驱动），后续可按需执行。

## Next Steps
- 若需运行完整 E2E/单测，请确保 ChromeDriver 配置好，或为涉及外部 API 的用例添加跳过条件。

## Linked Commits
- (pending)
