---
id: 2025-12-09T05-53-11Z-google-models
date: 2025-12-09T05:53:11Z
participants: [human, codex]
models: [gpt-4o-mini]
tags: [backend, api, ai-models]
related_paths:
  - ai-pic-backend/app/services/providers/google_provider.py
summary: "Improve Google model listing fallback to Generative Language API"
---
## User Prompt
现在只获得了openai 的，现在处理google的。

## Goals
- Ensure Google models show up even when Vertex `aiplatform` listing fails.
- Add a fallback to Generative Language API model listing.

## Changes
- Enhanced `GoogleProvider.fetch_remote_models` to first call Vertex `aiplatform` and, on failure/empty response, fall back to `https://generativelanguage.googleapis.com/v1beta/models`, reusing API key headers. Model inference and capability mapping reused for both sources.

## Validation
- `pytest tests/test_ai_service.py -q` (pass).

## Next Steps
- Consider caching remote model lists per provider to reduce latency.
- Add automated integration test hitting `/api/v1/ai/models/available` with mocked Google responses.

## Linked Commits
- (this commit)
