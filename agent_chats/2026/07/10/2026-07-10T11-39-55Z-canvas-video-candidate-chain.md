---
id: 2026-07-10T11-39-55Z-canvas-video-candidate-chain
date: "2026-07-10T11:39:55Z"
participants:
  - user
  - codex
models:
  - gpt-5-codex
tags:
  - canvas
  - backend
  - video-candidates
  - task-artifacts
related_paths:
  - ai-pic-backend/app/services/production_canvas/media_execution.py
  - ai-pic-backend/app/services/storyboard/video_generation_queue.py
  - ai-pic-backend/app/services/video/video_task_polling_parent_task.py
  - ai-pic-backend/tests/integration/test_production_canvas_media_api.py
  - ai-pic-backend/tests/unit/services/video/test_video_task_polling_parent_task.py
summary: Bind the latest canvas image candidate into storyboard video generation and persist the completed parent video artifact reference.
---

## User Prompt

继续完善无限画布功能；当前整体链路还是断的。可以拉起 dev_in_docker 并用内置浏览器检验。

## Goals

- Make `Video Candidates` consume the latest image candidate generated by the upstream canvas node.
- Give a completed storyboard video parent task a stable artifact reference that the canvas tracker can restore.
- Prove the handoff through a real provider task, persisted storyboard output, browser refresh, and full page reload.

## Changes

- Added explicit per-frame video selections from the latest nonempty `start_image_urls` candidate while leaving the regular storyboard primary-image semantics unchanged.
- Exposed the selected candidate count in the production canvas execution output.
- Recorded `storyboard_videos:<script_id>:<count>` on completed ordinary storyboard video parent tasks.
- Excluded failed parents and timeline rework tasks from the storyboard artifact reference.
- Added focused integration and parent status regression coverage.

## Validation

1. Local checks:

- `cd ai-pic-backend && python -m pytest tests/integration/test_production_canvas_media_api.py tests/unit/services/video/test_video_task_polling_parent_task.py tests/unit/services/video/test_video_task_polling_service.py -q` -> pass, 7 tests.
- Corrected `python -m compileall -q ...` from the backend directory after an initial repository-root invocation used invalid relative paths; the corrected command passed.
- `cd ai-pic-backend && python run_tests.py quick --no-setup` -> pass, 2406 passed, 77 skipped, 20 deselected.
- `cd ai-pic-backend && pytest` -> pass, 2416 passed, 88 skipped.
- `python scripts/check_repo_docs.py` and scoped `python scripts/check_repo_contracts.py --mode diff ...` -> pass.
- `git diff --check` -> pass.
- Scoped `pre-commit run --files ...` -> pass, including Ruff, Black, isort, repository docs/contracts, ledger enforcement, and the backend quick hook.
- `BUILD_PUSH=false BUILD_PLATFORMS=linux/amd64 ./docker/build_prod_images.sh` -> pass; backend and frontend production images built locally without push.

2. Browser and runtime validation:

- Entry URL: `http://localhost:8089/canvas?run_id=48d62cd56e1646c4b3f0c77c1a3cd4a6` in the Codex in-app browser against `docker/docker-compose.dev.yml`.
- Initial Volcengine `Task #6272` failed with HTTP 403 `AccountOverdueError`; this was an external billing failure, not accepted as canvas completion evidence.
- MiniMax `Task #6273` completed, but worker logs proved it still used the old primary image dated `20260428`; that run reproduced the canvas candidate-selection break.
- After the fix and worker restart, browser execution created parent `Task #6274` and child `VideoGenerationTask #222`, provider task `418324993474819`.
- Parent and child parameters both used the latest candidate `https://resource.lets-gpt.com/ai-generated/storyboard/image/20260710/111338/6c828b8e.png`; the worker log showed the same URL in the provider request.
- MiniMax completed and uploaded `https://resource.lets-gpt.com/ai-generated/videos/video/20260710/113524/b0e6a22e.mp4`. Script `130` frame `0` stored it as the primary video and appended it to `video_urls`.
- Parent `Task #6274` completed with `result_file_path: storyboard_videos:130:1`.
- Refreshing all tasks and a full page reload retained Task #6274, `selected_candidate_count: 1`, the result reference, and the restored run state. Browser console contained no warnings or errors.
- The in-app browser exposed no network-inspection capability and its Resource Timing list was empty; decisive request evidence came from the persisted parent/child parameters and worker provider log instead of a claimed Network-panel check.
- Screenshots: `artifacts/runs/canvas-video-chain-20260710T113805Z/video-candidates-restored.jpg` and `artifacts/runs/canvas-video-chain-20260710T113805Z/video-candidates-restored-full.jpg`.

3. Conflict signals and corrections:

- Initial successful provider output from Task #6273 did not prove the intended chain because its child task used the stale April image.
- The corrected Task #6274 used the latest July candidate and also supplied a parent artifact reference visible after restore.

## Next Steps

- Continue from the restored storyboard video into the Timeline skill and report evidence path.
- Define explicit partial-failure semantics for storyboard image candidates when one provider call fails but usable candidates are persisted.

## Linked Commits

Pending.
