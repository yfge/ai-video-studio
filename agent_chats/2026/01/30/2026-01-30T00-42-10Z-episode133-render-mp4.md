---
id: 2026-01-30T00-42-10Z-episode133-render-mp4
date: 2026-01-30T00:42:10Z
participants: [human, codex]
models: [gpt-5]
tags: [validation, e2e, video, ffmpeg, google, veo, audio]
related_paths:
  - tasks.md
summary: "为 Episode 133 生成全量分镜视频并合成两版成片 MP4（Veo 音频 / 自有 TTS 音轨）。"
---

## User Prompt

继续完成这一集的创作：生成所有分镜视频，并合并成整体 MP4 两版——一版使用生成视频自带音频，一版使用我们自己的音轨；并做一致性检验。

## Goals

- 补齐 `script_id=117` 的全部分镜视频（19 帧）。
- 基于 `storyboard.frames[*].duration_seconds`（总时长 200s）将分镜片段拼接为单集成片：
  - 版本 A：使用 Veo 生成视频自带音频
  - 版本 B：替换为 Episode TTS 音轨（`episode_id=133`）
- 校验一致性：帧数、时长、画幅、音轨对齐、产物可下载。

## Changes

- 更新任务看板：`tasks.md` 增补并勾选“基于 storyboard.frames 合成两版 MP4 并上传 OSS”的验证项。
- 新增本次验证记录：`agent_chats/2026/01/30/2026-01-30T00-42-10Z-episode133-render-mp4.md`。

## Validation

- Storyboard images（Banana Pro）：
  - `POST /api/v1/scripts/117/storyboard/generate-images` → task_id=5888（completed）
  - 结果：`script.extra_metadata.storyboard.frames` 19/19 均具备 `image_url`
- Storyboard videos（Veo 3.1）：
  - `POST /api/v1/scripts/117/storyboard/generate-video`（frames=18）→ task_id=5889（failed）
    - 失败原因定位（DB `video_generation_tasks`）：frame 8 “完成但未返回视频内容”；frame 17 “Internal error”
  - retry：`POST /api/v1/scripts/117/storyboard/generate-video`（frames=[8,17]）→ task_id=5890（completed）
  - 结果：`script.extra_metadata.storyboard.frames` 19/19 均具备 `video_url`
  - 抽检全部 19 个 mp4：分辨率均为 720x1280（9:16），并且均包含 audio stream
- 成片合成（FFmpeg，本地）：
  - 下载全部 19 个视频片段到 `/tmp/e2e_media/episode133/clips/`
  - 按每帧 `duration_seconds` 进行剪裁/补帧（freeze last frame）并 concat 成 200s（beats 对齐）：
    - 版本 A（保留视频自带音频）：`episode133_video_gen_audio.mp4`
    - 版本 B（替换 Episode TTS 音轨）：`episode133_video_our_audio.mp4`（音轨来源：`http://resource.lets-gpt.com/episode-dialogue/episodes/audio/20260129/234036/b6342fbe.mp3`）
- 上传 OSS/CDN（backend 容器内调用 `media_persistence.upload_bytes`）：
  - 版本 A（generated_audio）：`http://resource.lets-gpt.com/ai-generated/episode-renders/video/20260130/003917/95bf2d0a.mp4`
  - 版本 B（tts_audio）：`http://resource.lets-gpt.com/ai-generated/episode-renders/video/20260130/003919/58aa15ef.mp4`
- 一致性校验：
  - beats=19 与 frames=19；逐帧 `end_ms-start_ms` 对齐（差异 <= 50ms：0 条）
  - Episode speakers 集合为 `{老拐, 文闻, 旁白, None}`（无未知命名角色）
  - 已将渲染结果写入 `episode_id=133` 的 `extra_metadata.episode_renders`（包含两版 URL）

## Next Steps

- 将“concat + audio replace”升级为正式的渲染链路（RenderJob/Timeline Spec 落库、可续跑、可重渲）。
- 对长时段 beats（>8s）改为“自动切分多 clip”而非 freeze 补帧，以提升观感与声音一致性。
- 修复 Banana Pro 在 Vertex endpoint 返回 404 的降级路径可观测性（目前 fallback 到 Gemini API 成功，但日志易误判）。

## Linked Commits

- (pending) chore(tasks): document episode133 render outputs
