export type ModelUiValue = Partial<{
  size: string;
  aspect_ratio: string;
  resolution: string;
  ratio: string;
  duration: number;
  watermark: boolean;
  camera_fixed: boolean;
  camera_control_text: string;
  width: number;
  height: number;
}>;
