import { describe, it } from 'node:test'

// Placeholder E2E scaffold: enable when backend frames expose beat_id/shot_id metadata.
describe.skip('Storyboard frames align to structured beats/shots (pending backend links)', () => {
  it('should map frames to beat/shot identifiers and preserve ordering', async () => {
    // TODO: Implement once storyboard frame payload includes beat_id/shot_id.
    // Steps:
    // 1) Seed a script with scenes/beats/shots.
    // 2) Trigger storyboard generation and wait for frames.
    // 3) Assert frames carry beat_id/shot_id and match counts per scene.
  })
})
